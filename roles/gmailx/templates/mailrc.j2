# set smtp for mailx

# gmail account (default)
# $ mailx -s "subject line" -a /path/attachment recipient@email.com < /path/body.txt
set from="{{ gmailx.user }}"
set sendmail="/usr/bin/msmtp"
set message-sendmail-extra-arguments="-a gmail"
